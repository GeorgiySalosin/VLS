<Window x:Class="VLSGame.Views.Lobby"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:VLSGame.ViewModels"
        xmlns:models="clr-namespace:VLSGame.Models"
        xmlns:local="clr-namespace:VLSGame.Views"
        mc:Ignorable="d"
        Title="Game" Height="1080" Width="1920">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="32"/>
            <Setter Property="FontSize" Value="60"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
        </Style>
        <Style x:Key="ModeButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="128"/>
            <Setter Property="Height" Value="48"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="60"/>
            <Setter Property="FontFamily" Value="Bahnschrift"/>
        </Style>

        
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="1428"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Content="Start" Width="428" Height="132"/>
            <Button Grid.Row="1" Content="select mode" Height="200" Margin="32,0,32,32"/>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Content="X" Width="92" Height="92" VerticalAlignment="Top" HorizontalAlignment="Right"/>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Map"/>
                <ItemsControl Grid.Row="1" ItemsSource="{Binding Maps}"> <!--MapButtons-->
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" 
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Width="360" Height="240" Margin="0,0,32,32">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border>
                                            <Border.Background>
                                                <ImageBrush ImageSource="{Binding MapBackgroundImage}" Stretch="UniformToFill"/>
                                            </Border.Background>
                                            <ContentPresenter
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition Height="auto"/>
                                    </Grid.RowDefinitions>
                                    <Image Grid.Row="0"
                                                Source="{Binding Checkmark}"
                                                Width="32" Height="32" 
                                                VerticalAlignment="Top" HorizontalAlignment="Right"/>
                                    <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" 
                                                        FontSize="20" Foreground="White"/>
                                        <TextBlock Text="{Binding Subtitle}" 
                                                        FontSize="12" Foreground="LightGray"/>
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Grid.Row="2" Text="Mode"/>
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Style="{StaticResource ModeButton}"/>
                    <Button Grid.Column="1" Style="{StaticResource ModeButton}" Margin="0,32,32,32"/>
                </Grid>
            </Grid>
            <Button Grid.Row="2" Content="Ok" Width="268" Height="92" VerticalAlignment="Bottom" HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</Window>
